local session_status_ok, sessions = pcall(require, "mini.sessions")
if not session_status_ok then
	return
end

local starter_status_ok, starter = pcall(require, "mini.starter")
if not starter_status_ok then
	return
end

sessions.setup()
starter.setup({
	items = {
		starter.sections.sessions(30, true),
		-- starter.sections.recent_files(10, false, false),
		starter.sections.builtin_actions(),
	},
})
